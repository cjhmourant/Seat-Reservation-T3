import { createGlobalStyle } from "styled-components";

export const CssVariables = createGlobalStyle`
  :root {
    --mobile-seat-width: 50px;
    --mobile-seat-height: 50px;
    --desktop-seat-width: 100px;
    --desktop-seat-height: 100px;
  }
`;
